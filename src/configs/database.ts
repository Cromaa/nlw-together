const DATABASE_NAME = '@gameplay';

//@nomeapp: coleção

const COLLECTION_USERS = `${DATABASE_NAME}:user`;
const COLLECTION_APPOITMENTS = `${DATABASE_NAME}:appointments`;

export {
    COLLECTION_USERS,
    COLLECTION_APPOITMENTS
}
